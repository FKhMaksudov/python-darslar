# def hello():
#     print("Hello, World!")

# def goodbye():
#     print("Goodbye, World!")
# def greet(name):
#     print(f"Hello, {name}!") 

'''k = 42
l = 57
m = 83
n = 14
# x * 2 ** 3 / 2 + y
print(k * 2 ** 3 / 2 + l)
print(m * 2 ** 3 / 2 + n)
print(k * 2 ** 3 / 2 + m)
print(l * 2 ** 3 / 2 + n) #Чтобы не было такое повторение кода, можно использовать функции.

def calculate(x,y): #Функция def в Python используется, чтобы создать свою собственную команду (набор инструкций), которую можно многократно вызывать.
    print(x * 2 ** 3 / 2 + y)
calculate(k, l)
calculate(m, n)
calculate(k, m)
calculate(l, n)'''

o = 6
# if else:
# печатать "привет" если значение переменной o меньше 5, а если больше 5, то печатать "пока"
# если o меньше 5, то напечатать "привет", иначе напечатать "пока"
# if (если) o < 5 then (:) print("привет") else (иначе) print("пока")
if o < 5:
    print("привет")
else:
    print("пока")

o = 6
# if else:
# печатать "привет" если значение переменной o меньше 5,а если значение o от 5 до 10,то печатать привет привет, иначе печатать "пока"
# если o меньше 5, то напечатать "привет",а если (иначе если)от 5 до 10,то напечатать "привет привет", иначе напечатать "пока"
# if (если) o < 5 then (:) print("привет") else (иначе) print("пока")
if o < 5:
    print("привет")
elif o < 10 and o >= 5:
    print("привет привет")
else:
    print("пока")

def work():
    print("Работаю...")
    if  a == b
        print("a равно b")
    elif a > b:
        print("a больше b")
    else:
        print("a меньше b") 
def work(a, b):
    print("Работаю...")
    if a == b:
        print("a равно b")
    elif a > b:
        print("a больше b")
    else:
        print("a меньше b")

        